# ===================================
# Configurações do Servidor
# ===================================
PORT=3002
NODE_ENV=development

# ===================================
# Configurações de E-mail
# ===================================
# Serviço de e-mail (gmail, outlook, etc.)
EMAIL_SERVICE=gmail
# E-mail do remetente
EMAIL_USER=seu-email@gmail.com
# Senha do e-mail ou senha de app (para Gmail, use senha de app)
EMAIL_PASS=sua-senha-gerada
# Nome e e-mail que aparecerá no remetente
EMAIL_FROM="RealCred+ <seu-email@gmail.com>"
# E-mail que receberá os formulários preenchidos
EMAIL_TO=contato@realcredmais.com.br
# E-mail de resposta (opcional)
EMAIL_REPLY_TO=nao-responda@realcredmais.com.br

# ===================================
# Configurações de Segurança
# ===================================
# Taxa de limite de requisições (em milissegundos)
RATE_LIMIT_WINDOW_MS=900000  # 15 minutos
# Número máximo de requisições por janela de tempo
RATE_LIMIT_MAX=100
# Chave secreta para sessões (gerar com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=substitua-por-uma-chave-secreta-forte
# Tempo de expiração do token JWT (opcional)
# JWT_EXPIRES_IN=1d

# ===================================
# Configurações de CORS
# ===================================
# URLs permitidas separadas por vírgula
ALLOWED_ORIGINS=http://localhost:3000,https://realcredmais.com.br
# Métodos permitidos
ALLOWED_METHODS=GET,POST,PUT,PATCH,DELETE,OPTIONS
# Cabeçalhos permitidos
ALLOWED_HEADERS=Content-Type,Authorization,X-Requested-With,Accept,Origin
# Credenciais permitidas
ALLOW_CREDENTIALS=true

# ===================================
# Configurações de Log
# ===================================
# Nível de log (error, warn, info, debug)
LOG_LEVEL=info
# Formato do log (combined, common, dev, short, tiny)
LOG_FORMAT=combined
# Arquivo de log (opcional)
# LOG_FILE=logs/app.log

# ===================================
# Configurações de Banco de Dados (opcional)
# ===================================
# MongoDB
# MONGODB_URI=mongodb://usuario:senha@localhost:27017/realcredmais?authSource=admin
# Redis para cache/sessão
# REDIS_URL=redis://localhost:6379
# PostgreSQL
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/realcredmais

# ===================================
# Chaves de API (opcional)
# ===================================
# Google reCAPTCHA v3
# RECAPTCHA_SECRET_KEY=sua-chave-aqui
# Google Maps
# GOOGLE_MAPS_API_KEY=sua-chave-aqui
# Google Analytics
# GA_TRACKING_ID=UA-XXXXX-Y

# ===================================
# Configurações de Upload
# ===================================
# Tamanho máximo do arquivo (em bytes)
# MAX_FILE_SIZE=5242880  # 5MB
# Diretório para uploads
# UPLOAD_DIR=./public/uploads
# Tipos de arquivo permitidos (separados por vírgula)
# ALLOWED_FILE_TYPES=image/jpeg,image/png,application/pdf

# ===================================
# Configurações de Cache
# ===================================
# Tempo de vida do cache em segundos
# CACHE_TTL=3600  # 1 hora
# Ativar/desativar cache
# CACHE_ENABLED=true

# ===================================
# Configurações de Monitoramento (opcional)
# ===================================
# Sentry DSN para monitoramento de erros
# SENTRY_DSN=sua-dsn-do-sentry
# New Relic License Key
# NEW_RELIC_LICENSE_KEY=sua-chave-new-relic

# ===================================
# Configurações de Produção (sobrescreve as configurações acima quando NODE_ENV=production)
# ===================================
if [ "$NODE_ENV" = "production" ]; then
  # Exemplo: Desativar logs detalhados em produção
  LOG_LEVEL=warn
  # Exemplo: Usar HTTPS em produção
  # PROTOCOL=https
  # DOMAIN=realcredmais.com.br
  # Exemplo: Configurações específicas de produção
  # DATABASE_URL=postgresql://usuario:senha@db-prod:5432/realcredmais
fi
